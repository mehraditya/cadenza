# Cadenza dependencies.
#
#   git clone ... && cd cadenza
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# This file installs the cadenza package itself (-e .) plus everything its
# source actually imports, pinned to known-good versions. It is derived from
# the source's real import closure, not a raw `pip freeze` of the dev venv
# (which also held unrelated research tooling — lerobot, genesis, mediapipe —
# whose conflicting pins made a clean reinstall impossible).

# The cadenza package itself (maps src/ -> import cadenza, installs the CLI).
-e .

# ── Core: import cadenza, the simulator, and every robot (Go1 / G1 / arm) ─────
# These three alone are enough to run the arm and the rest of the sim/CLI.
numpy==2.4.4
mujoco==3.8.0
click==8.3.3

# ── Plotting (benchmarks / trajectory plots) ──────────────────────────────────
matplotlib==3.10.9

# ── Multi-robot coordination: cadenza.connect(...) / term.coordinate(...) ──────
fastmcp==3.4.2

# ── VLA guardian + parser action head + depth/vision (AI features, heavy) ─────
# Pulls ~GBs of CUDA/torch wheels. Drop these three if you only need the
# action library + simulation (e.g. just the arm) and not the AI features.
torch==2.10.0
transformers==5.3.0
pillow==12.2.0

# ── Optional, NOT on PyPI — install yourself if you need them ──────────────────
# Real-robot deploy over DDS needs the Unitree SDK:  unitree_sdk2py
# World-model adapters expect your own model package importable as: ai_models
